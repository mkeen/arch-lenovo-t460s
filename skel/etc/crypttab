# crypttab: mappings for encrypted partitions
#
# Each mapped device will be created in /dev/mapper, so your /etc/fstab
# should use the /dev/mapper/<name> paths for encrypted devices.
#
# The Arch specific syntax has been deprecated, see crypttab(5) for the
# new supported syntax.
#
# NOTE: Do not list your root (/) partition here, it must be set up
#       beforehand by the initramfs (/etc/mkinitcpio.conf).

# <name>       <device>   <password>            <options>
sda2           /dev/sda2  /dev/urandom          swap,cipher=aes-cbc-essiv:sha256,size=256,hash=plain
sda3	       /dev/sda3  /dev/urandom          tmp,cipher=aes-cbc-essiv:sha256,size=256,hash=plain
sda4           /dev/sdb4  /etc/unsecure.key     hash=plain
sda6           /dev/sda6  /etc/unsecure.key     hash=plain
